<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- Sync Attendance Devices -->
        <record id="ir_cron_sync_attendance_devices" model="ir.cron">
            <field name="name">Sync Attendance Devices</field>
            <field name="model_id" ref="model_attendance_device"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_attendance()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Auto-Close Stale Attendances -->
        <record id="ir_cron_auto_close_attendances" model="ir.cron">
            <field name="name">Auto-Close Stale Attendances</field>
            <field name="model_id" ref="model_attendance_processor"/>
            <field name="state">code</field>
            <field name="code">model.cron_auto_close_stale_attendances()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(hours=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
        
    </data>
</odoo>